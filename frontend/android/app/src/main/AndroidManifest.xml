<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- MainActivity is NOT the launcher entry itself: the MAIN/LAUNCHER lives on
             the theme aliases below (exactly one enabled at a time) so the user can
             match the app icon to the active theme. MainActivity keeps the deep-link
             VIEW filter and remains every alias's targetActivity. -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.clevercode.sempa" />
            </intent-filter>
            <!-- Share-to-Sempa: accept text/links shared from other apps. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Themed launcher icons. Exactly one alias is enabled at a time; the app
             switches them (WidgetBridge.setAppIcon) when the user opts to match the
             icon to a new theme. Terracotta is the default. -->
        <activity-alias android:name=".IconTerracotta" android:targetActivity=".MainActivity"
            android:exported="true" android:enabled="true" android:label="@string/title_activity_main"
            android:icon="@mipmap/ic_launcher_terracotta" android:roundIcon="@mipmap/ic_launcher_terracotta_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias android:name=".IconForest" android:targetActivity=".MainActivity"
            android:exported="true" android:enabled="false" android:label="@string/title_activity_main"
            android:icon="@mipmap/ic_launcher_forest" android:roundIcon="@mipmap/ic_launcher_forest_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias android:name=".IconPlum" android:targetActivity=".MainActivity"
            android:exported="true" android:enabled="false" android:label="@string/title_activity_main"
            android:icon="@mipmap/ic_launcher_plum" android:roundIcon="@mipmap/ic_launcher_plum_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias android:name=".IconSlate" android:targetActivity=".MainActivity"
            android:exported="true" android:enabled="false" android:label="@string/title_activity_main"
            android:icon="@mipmap/ic_launcher_slate" android:roundIcon="@mipmap/ic_launcher_slate_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias android:name=".IconOled" android:targetActivity=".MainActivity"
            android:exported="true" android:enabled="false" android:label="@string/title_activity_main"
            android:icon="@mipmap/ic_launcher_oled" android:roundIcon="@mipmap/ic_launcher_oled_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias android:name=".IconOcean" android:targetActivity=".MainActivity"
            android:exported="true" android:enabled="false" android:label="@string/title_activity_main"
            android:icon="@mipmap/ic_launcher_ocean" android:roundIcon="@mipmap/ic_launcher_ocean_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Widget receivers -->
        <receiver
            android:name=".widget.SempaSmallWidgetReceiver"
            android:label="@string/widget_small_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sempa_small_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.SempaMediumWidgetReceiver"
            android:label="@string/widget_medium_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sempa_medium_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.SempaLargeWidgetReceiver"
            android:label="@string/widget_large_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sempa_large_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.SempaFocusWidget"
            android:label="@string/widget_pomodoro_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sempa_pomodoro_widget_info" />
        </receiver>

        <!-- FCM Messaging Service -->
        <service
            android:name=".SempaMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Ongoing focus-timer notification; its countdown survives app close. -->
        <service
            android:name=".FocusTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="focus_timer" />
        </service>

    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Exact alarms so task reminders fire at the precise minute on Android 13+.
         Without these, @capacitor/local-notifications' exact (allowWhileIdle)
         schedule is downgraded/blocked and the reminder never appears.
         USE_EXACT_ALARM covers 13+ (granted at install for an alarm/reminder app);
         SCHEDULE_EXACT_ALARM covers 12. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Ongoing focus-timer notification (foreground service). SPECIAL_USE is the
         honest fit: a persistent, user-initiated stopwatch, not media/location/etc. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
</manifest>
